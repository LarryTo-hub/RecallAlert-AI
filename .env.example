# Copy this file to `.env` and fill in values for your local/dev environment.
# IMPORTANT: Do NOT commit a filled `.env` file with secrets to version control.

# General / Safety settings
# When ALLOW_SEND is false the notifier will only log messages (dry-run).
ALLOW_SEND=false
# If set to true, SMS / email sends may be performed depending on notifier code.
# Use ALLOW_SEND=false for local development and demos.

# App logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Notification behavior
# Choose default behavior for notifications: "global" (all users) or "personalized" (only matched receipts)
NOTIFICATION_MODE=global
# Default from address for email-based notifications
DEFAULT_FROM_EMAIL=recalls@example.com

# SMTP (for email-to-SMS or other email notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=super-secret

# Twilio (optional)
# Twilio is a cloud communications platform (https://www.twilio.com/) that provides APIs
# for sending SMS, MMS and making voice calls. We may want to use Twilio to deliver
# timely SMS alerts directly to users' phones because it's reliable, scalable, and
# supports delivery status callbacks. If you prefer email-to-SMS gateways or webhooks,
# leave these blank.
# Leave blank to disable Twilio sending
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+15551234567

# LLM / AI provider (optional)
# If you will add an AI agent, store provider selection and API keys here.
# Do NOT commit real API keys to the repo — keep them only in a local `.env` or a secrets manager.
# MODEL_PROVIDER: e.g. "openai", "azure", "anthropic", "other"
MODEL_PROVIDER=openai

# OpenAI (if using OpenAI-hosted models)
# Put your API key here. Example: OPENAI_API_KEY=sk-xxx
OPENAI_API_KEY=
# Default model name to use for generation/summarization. Pick a model available to your account,
# e.g. "gpt-3.5-turbo", "gpt-4o", or a smaller model for cost savings.
OPENAI_MODEL=gpt-3.5-turbo

# Azure OpenAI (optional): endpoint and key if using Azure-hosted models
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=
AZURE_OPENAI_DEPLOYMENT_NAME=

# Anthropic (optional)
ANTHROPIC_API_KEY=

# LLM runtime settings
# Max time (seconds) to wait for the model response
LLM_TIMEOUT_SECONDS=30
# Max tokens to request for completions/summaries (adjust by model)
LLM_MAX_TOKENS=1024

# Webhook (optional)
# A webhook URL that can receive recall payloads (POST). Example: https://hooks.example.com/recalls
WEBHOOK_URL=

# Database
# For local dev a SQLite file is fine. For production use Postgres or MySQL and set a proper URL.
# Examples:
#   sqlite: sqlite:///recalls.db
#   postgres: postgresql://user:pass@host:5432/recalls
DATABASE_URL=sqlite:///recalls.db

# Scheduler / polling
# Poll interval in minutes for background fetcher (used by APScheduler jobs)
FETCH_INTERVAL_MINUTES=60

# Misc / Feature flags
# If true, store a history of sent notifications (requires DB schema support)
STORE_NOTIFICATION_HISTORY=false

# Toggle more verbose demo-friendly output
DEMO_MODE=true

# End of `.env.example` — copy to `.env` and fill sensitive values locally.
